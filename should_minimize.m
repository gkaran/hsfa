function [minim] = should_minimize
    % If we want to perform a minimization procedure then set minim to 0
    % If we want to perform a maximization procedure then set minim to 1
    
    % Min <- minim = 1
    % Max <- minim = 0
    minim = 1;
end